Libraries used: iostream, vector, algorithm, cctype, cstring, chrono, fstream

To run Quick Sort Worst Case, use command #2 with Input_order = -qsortworst:
# Prototype
> groupid.[exe] -a quick-sort [Input size] -qsortworst [Output param]
# For example:
> 01.[exe] -a quick-sort 500000 -qsortworst -both
